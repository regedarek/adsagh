!!!
%html
  %head
    %title
      = yield(:title)
      || TablicAGH.pl
    %meta{"http-equiv"=>"Content-Type", :content=>"text/html; charset=utf-8"}/
    = stylesheet_link_tag :application
    = javascript_include_tag "http://code.jquery.com/jquery-1.7.1.min.js", :categories
    = csrf_meta_tag
    = yield(:head)

  %body
    #container
      #sidebar
        %br
        =yield :content_menu
      #content
        - if current_user
          Zalogowany jako
          = current_user.username
          = link_to "Wyloguj", wyloguj_path

        - flash.each do |name, msg|
          = content_tag :div, msg, :id => "flash_#{name}"
        
        - if show_title?
          %h1= yield(:title)
        
        = yield



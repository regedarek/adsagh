%h1.title
  Lista ogłoszeń
- if @ads.empty?
  brak ogłoszeń w bazie
- else
  - @ads.each do |ad|
    #title.ad= link_to(ad.title, ad_path(ad))
    %br

- content_for :content_menu do
  = link_to "Dodaj ogłoszenie", new_ad_path
  = link_to "Kategorie", categories_path
  = link_to "Google", "http://localhost:3000/auth/google_oauth2"
  = link_to "Facebook", "http://0.0.0.0:3000/auth/facebook"

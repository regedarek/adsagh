- title "Kategorie"

%h2 Widok scalony
=render :partial => "bar"


%h2 Drzewo Kategori
.categories_tree
  .description
    Tylko tutaj zamienie i będzie możliwość edytowania z drzewa a nie z widoku scalonego.
  .tree
    = nested_categories_tree @categories

- content_for :content_menu do
  = link_to t(".new_category"), new_category_path
  = link_to t(:root_path), root_path


//ten kod jest brzydki ale to tylko chcialem zeby działało
%br
=link_to "test linka do categori AGD", categories_path(:category=>"2")
%br
%br
%strong Tu wyswietla wszystkie dzieci tej categori:
%br
%var=@categoria
%br
%br
%strong A tutaj ogłoszenia z tej kategori i jej dzieci:
%br
-@ads.each do |ad|
  %var  
    = link_to ad.title, ad_path(ad)
    Categoria nr:
    = ad.category_id
  %br
